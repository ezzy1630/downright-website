# Markdown formats supported by Downright

Downright opens `.md`, `.markdown`, `.mdown`, `.mkd`, `.mdx`, `.mdc`, `.qmd`, and `.rmd` files.

It renders CommonMark/GFM structure, tables, tasks, callouts, footnotes, code, math, Mermaid, front matter, and file-path references while preserving the source text.

# Markdown workflow for Claude Code on macOS

Keep plans, `CLAUDE.md`, README files, and release notes as ordinary Markdown in the repository. Downright gives those files a native reading and review surface while Claude Code works on them.

When Claude Code rewrites the open file, Downright marks the changed words and lets you inspect the result before accepting it.
